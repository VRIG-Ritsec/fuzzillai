{
  "body": "// Register allocator crash patterns in V8 compiler\n\n// From compiler/backend/register-allocator.cc:\n\n// 1. DCHECK failures for invariant violations:\n// Line 74: DCHECK_IMPLIES(hint == nullptr, hint_type == UsePositionHintType::kNone);\n// Line 94: DCHECK(pos_.IsValid());\n// Line 130: UNREACHABLE();\n// Line 145: DCHECK(op.IsStackSlot() || op.IsFPStackSlot());\n// Line 152: UNREACHABLE();\n// Line 156: DCHECK_NOT_NULL(use_pos);\n// Line 162: DCHECK_NOT_NULL(use_pos);\n// Line 169: DCHECK_IMPLIES(type == UsePositionType::kRequiresSlot, !register_beneficial);\n// Line 170: DCHECK_EQ(kUnassignedRegister, AssignedRegisterField::decode(flags_));\n// Line 188: DCHECK(AllocatedOperand::IsSupportedRepresentation(rep));\n// Line 196: SLOW_DCHECK(std::is_sorted(positions().begin(), positions().end(),\n// Line 202: DCHECK_LE(Start(), pos->pos());\n// Line 203: DCHECK_LE(pos->pos(), End());\n// Line 204: DCHECK_NE(interval, intervals().end());\n// Line 210: DCHECK_NE(interval, intervals().end());\n// Line 216: DCHECK(!intervals().empty());\n// Line 217: DCHECK_EQ(intervals().front().start(), Start());\n// Line 222: DCHECK_LE(last_end, interval->start());\n// Line 225: DCHECK_EQ(last_end, End());\n// Line 230: DCHECK(!HasRegisterAssigned() && !spilled());\n// Line 235: DCHECK(HasRegisterAssigned() && !spilled());\n// Line 240: DCHECK_NOT_NULL(next_);\n// Line 253: CHECK_EQ(positions_span_.end(), next_->positions_span_.begin());\n// Line 265: DCHECK(spilled());\n// Line 271: DCHECK(!spilled());\n// Line 272: DCHECK(!TopLevel()->HasNoSpillType());\n// Line 288: DCHECK_LE(current_hint_position_index_, positions_span_.size());\n// Line 292: DCHECK_GE(positions_span_[current_hint_position_index_]->pos(),\n// Line 293:            positions_span_.first()->pos());\n// Line 294: DCHECK_LE(positions_span_[current_hint_position_index_]->pos(), End());\n// Line 316: CHECK_EQ(pos_it, pos_check_it);\n// Line 374: DCHECK(!IsEmpty());\n// Line 376: DCHECK(!spilled());\n// Line 380: DCHECK(spilled());\n// Line 381: DCHECK(!HasRegisterAssigned());\n// Line 384: DCHECK(!op->IsUnallocated());\n// Line 392: DCHECK_NE(current_interval_, intervals_.end());\n// Line 405: DCHECK_LE(intervals_.begin(), to_start_of);\n// Line 406: DCHECK_LT(to_start_of, intervals_.end());\n// Line 407: DCHECK_NE(current_interval_, intervals_.end());\n// Line 415: DCHECK(Start() < position);\n// Line 416: DCHECK(End() > position);\n// Line 431: DCHECK_NE(split_interval, intervals_.end());\n// Line 441: DCHECK(!intervals_.empty());\n// Line 442: DCHECK(!result->intervals_.empty());\n// Line 502: DCHECK(Start() <= pos->pos() && pos->pos() <= End());\n// Line 506: DCHECK(spill_op.IsStackSlot() || spill_op.IsFPStackSlot());\n// Line 510: DCHECK(op.IsRegister() || op.IsFPRegister());\n// Line 583: DCHECK(interval + 1 == intervals().end() ||\n// Line 584:           interval[1].start() >= interval->start());\n// Line 595: DCHECK_LE(interval->start(), position);\n// Line 607: DCHECK_NE(interval, intervals().end());\n// Line 619: DCHECK_NE(interval, intervals().end());\n\n// 2. Critical CHECK failures that can crash:\n// Line 253: CHECK_EQ(positions_span_.end(), next_->positions_span_.begin());\n// Line 316: CHECK_EQ(pos_it, pos_check_it);\n// Line 1323: CHECK_EQ(live_ranges_size, data()->live_ranges().size());\n// Line 2426: CHECK_EQ(live_ranges_size, data()->live_ranges().size());\n// Line 2747: CHECK_EQ(initial_range_count, data()->live_ranges().size());\n\n// 3. UNREACHABLE() calls for unexpected code paths:\n// Line 130: UNREACHABLE();\n// Line 152: UNREACHABLE();\n// Line 1531: UNREACHABLE();\n// Line 1536: UNREACHABLE();\n// Line 1863: UNREACHABLE();\n// Line 4012: UNREACHABLE();\n\n// 4. Common crash scenarios:\n// - Invalid live range intervals (empty, malformed)\n// - Register assignment inconsistencies\n// - Spill state violations\n// - Position ordering violations\n// - Interval intersection errors\n// - Memory corruption in live range data structures",
  "context": [
    "compiler_crash_patterns",
    "memory_corruption_patterns"
  ],
  "explanation": "This entry documents crash patterns in the register allocator, which is a critical component of the V8 compiler backend. The register allocator has extensive DCHECK and CHECK macros that can trigger crashes when invariants are violated. Common crash scenarios include invalid live range intervals, register assignment inconsistencies, spill state violations, and memory corruption in live range data structures.",
  "file_line": "compiler/backend/register-allocator.cc:74-4012"
}