diff --git a/src/objects/feedback-vector.cc b/src/objects/feedback-vector.cc
index 1b74062970b..120315c4f2c 100644
--- a/src/objects/feedback-vector.cc
+++ b/src/objects/feedback-vector.cc
@@ -20,6 +20,7 @@
 #include "src/objects/hash-table-inl.h"
 #include "src/objects/map-inl.h"
 #include "src/objects/objects.h"
+#include "src/fuzzilli/cov.h"
 
 namespace v8::internal {
 
@@ -635,6 +636,7 @@ bool FeedbackNexus::ConfigureMegamorphic() {
   Tagged<MaybeObject> sentinel = MegamorphicSentinel();
   if (GetFeedback() != sentinel) {
     SetFeedback(sentinel, SKIP_WRITE_BARRIER, kClearedWeakValue);
+    cov_add_feedback_nexus(this);
     return true;
   }
 
@@ -646,6 +648,7 @@ void FeedbackNexus::ConfigureMegaDOM(const MaybeObjectDirectHandle& handler) {
   Tagged<MaybeObject> sentinel = MegaDOMSentinel();
 
   SetFeedback(sentinel, SKIP_WRITE_BARRIER, *handler, UPDATE_WRITE_BARRIER);
+  cov_add_feedback_nexus(this);
 }
 
 bool FeedbackNexus::ConfigureMegamorphic(IcCheckType property_type) {
@@ -659,6 +662,7 @@ bool FeedbackNexus::ConfigureMegamorphic(IcCheckType property_type) {
       feedback.first != sentinel || feedback.second != maybe_extra;
   if (update_required) {
     SetFeedback(sentinel, SKIP_WRITE_BARRIER, maybe_extra, SKIP_WRITE_BARRIER);
+    cov_add_feedback_nexus(this);
   }
   return update_required;
 }
