diff --git a/src/maglev/maglev-graph-builder.h b/src/maglev/maglev-graph-builder.h
index 49557da53e4..179f2b3c469 100644
--- a/src/maglev/maglev-graph-builder.h
+++ b/src/maglev/maglev-graph-builder.h
@@ -40,6 +40,7 @@
 #include "src/objects/bytecode-array.h"
 #include "src/objects/elements-kind.h"
 #include "src/objects/string.h"
+#include "src/fuzzilli/cov.h"
 
 namespace v8 {
 namespace internal {
@@ -1892,13 +1893,17 @@ class MaglevGraphBuilder {
     return compilation_unit_->feedback();
   }
   const FeedbackNexus FeedbackNexusForOperand(int slot_operand_index) const {
-    return FeedbackNexus(feedback().object(),
+    FeedbackNexus nexus = FeedbackNexus(feedback().object(),
                          GetSlotOperand(slot_operand_index),
                          broker()->feedback_nexus_config());
+    cov_add_feedback_nexus(&nexus);
+    return nexus;
   }
   const FeedbackNexus FeedbackNexusForSlot(FeedbackSlot slot) const {
-    return FeedbackNexus(feedback().object(), slot,
+    FeedbackNexus nexus = FeedbackNexus(feedback().object(), slot,
                          broker()->feedback_nexus_config());
+    cov_add_feedback_nexus(&nexus);
+    return nexus;
   }
   compiler::BytecodeArrayRef bytecode() const {
     return compilation_unit_->bytecode();
